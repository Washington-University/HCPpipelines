# Ignore Eclipse Project files
*.project
*.buildpath
*.pydevproject

# Ignore backup files generated by some editors
*~
*.*~

# Ignore gradient coefficients files
# These are sometimes proprietary and should not be 
# checked in to a repository.
*.grad

# nfs temporaries
.nfs*

#NHP templates - these are a lot of files, some of which are large (because the gca format isn't compressed), and are a separate download
/global/templates/NHP_NNP/
/global/templates/NHP_NNP_Templates*.zip

/Supplemental

